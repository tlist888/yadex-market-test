Написать SQL скрипт рандомного "выключателя":
Внутри скрипта задать рандомайзер, генерирующий число от 0 до 100, если сгенерированное число меньше 50, то возвращает 0, иначе 1.

SELECT IIF(RAND() * 100 < 50, 0, 1)